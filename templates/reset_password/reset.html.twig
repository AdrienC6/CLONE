{% extends 'layouts/base.html.twig' %}

{% block title %}Réinitialiser mon mot de passe{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8 mx-auto">
            <h1 class="mt-5 mb-5 text-center text-muted">Réinitialiser mon mot de passe</h1>
            {{ form_start(resetForm) }}
                {{ form_row(resetForm.plainPassword) }}
                <button type="submit" class="btn btn-primary btn-block">Réinitialiser mon mot de passe</button>
            {{ form_end(resetForm) }}
        </div>
    </div>
{% endblock %}
