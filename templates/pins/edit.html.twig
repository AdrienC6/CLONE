{% extends 'layouts/base.html.twig' %}

{% block title 'Modifier : ' ~ pin.title %}

{% block body %}
    <div class="row">
      <div class="col-md-6 mx-auto">
        <h1 class="text-center text-muted mb-3">Modifier : {{ pin.title }}</h1>
        {{ include('pins/_form.html.twig', {submitButtonText: 'Modifier'}) }}
        <p class="text-muted text-center mt-3"><a class="text-muted" href="{{path('app_home')}}">Annuler</a></p>
      </div>
    </div>

{% endblock %}
